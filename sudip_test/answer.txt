1. argument is the number passed in the function and parameter is the variable that receive the number

2. 10/3 gives floating value while 10//3 gives int value

3. range(1,10,2) return 1,3,5,7,9

4. using * with the parameter in the function

5. local variable can only be accessed in same function where it is defined while globle variable can be accessed anywhere in the program

6. name="John Smith"
    a. name[1] return o
    b. name[-2] return t ,second letter from right side
    c. name[1:-1] returns ohn Smit, starts with name[1] index till second last index, leaving the last index
    d. name.title() return first letter of string capital. output: John Smith
    e. name.strip() is used to remove spaces in left or right of string,it doesnot removes spaces between the string
    f. name.find("Smith") returns 5 ,find gives the index after which the Smith starts
    g. name.replace("j","k") gives same John Smith,. Its case sensitive so j and J are different.
    h. print("John" in name)  ,it returns true.

